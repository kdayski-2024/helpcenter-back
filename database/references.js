module.exports = function (models) {

    // models.Teacher.hasMany(models.Course, { foreignKey: 'id' });
    // models.Course.belongsTo(models.Teacher, { foreignKey: 'teacherId' })

    return models;
};